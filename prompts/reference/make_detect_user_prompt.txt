PAGE 5. Identify every BODY-TEXT bibliography citation occurrence that cites one of these extracted reference-list markers: ["[1]", "[2]", "[3]"]. The paper's bibliography uses a mixture of indexing styles. Report superscripts only when they clearly match a numeric entry in the allowed marker list. Detect only bibliography citation markers. Return every repeated occurrence separately. Expand grouped citations (e.g. [21–23], [26] [27], [6,7], [16, 9, 24]) into separate markers FIRST, then filter each expanded member against the allowed list. Do NOT drop a group just because its literal surface string is not in the allowed list. Return strict JSON.
